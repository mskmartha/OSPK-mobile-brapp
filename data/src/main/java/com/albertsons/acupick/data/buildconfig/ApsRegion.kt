package com.albertsons.acupick.data.buildconfig

enum class ApsRegion {
    /** Signifies west region (launched with MLP) */
    West,
    /** Signifies east region */
    East,
    /** Signifies canary (pilot) stores */
    Canary,
}
